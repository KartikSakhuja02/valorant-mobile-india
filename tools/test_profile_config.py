# =============================================================================
# SIMPLE GUIDE: Change these numbers to adjust your match history display
# =============================================================================
# 
# BIGGER NUMBER = Move RIGHT or DOWN
# SMALLER NUMBER = Move LEFT or UP
# 
# Size values: BIGGER = larger images/text, SMALLER = smaller images/text
# =============================================================================

# ----- MATCH HISTORY POSITION -----
MATCH_START_Y = 780        # Change this to move ALL matches UP (smaller) or DOWN (bigger)
MATCH_ROW_HEIGHT = 90      # Space between matches (bigger = more space)

# ----- MAP IMAGE -----
MAP_IMG_X = 100            # Move map LEFT (smaller) or RIGHT (bigger)
MAP_IMG_Y_OFFSET = 0       # Move map UP (negative) or DOWN (positive)
MAP_IMG_WIDTH = 100        # Make map WIDER (bigger number)
MAP_IMG_HEIGHT = 60        # Make map TALLER (bigger number)

# ----- AGENT ICON -----
AGENT_IMG_X = 220          # Move agent LEFT (smaller) or RIGHT (bigger)
AGENT_IMG_Y_OFFSET = -10   # Move agent UP (negative) or DOWN (positive)
AGENT_IMG_SIZE = 80        # Make agent icon BIGGER or SMALLER

# ----- TEAM SCORE (13-11) -----
SCORE_X = 320              # Move score LEFT (smaller) or RIGHT (bigger)
SCORE_Y_OFFSET = 20        # Move score UP (negative) or DOWN (positive)
SCORE_FONT_SIZE = 24       # Make score text BIGGER or SMALLER

# ----- K/D/A STATS (25/15/5) -----
KDA_X = 470                # Move K/D/A LEFT (smaller) or RIGHT (bigger)
KDA_Y_OFFSET = 20          # Move K/D/A UP (negative) or DOWN (positive)
KDA_FONT_SIZE = 24         # Make K/D/A text BIGGER or SMALLER

# ----- MAP NAME TEXT -----
MAP_NAME_X = 650           # Move map name LEFT (smaller) or RIGHT (bigger)
MAP_NAME_Y_OFFSET = 20     # Move map name UP (negative) or DOWN (positive)
MAP_NAME_FONT_SIZE = 24    # Make map name text BIGGER or SMALLER

# ----- COLORS -----
WIN_COLOR = "#00ff00"      # Green for wins
LOSS_COLOR = "#ff0000"     # Red for losses
KDA_COLOR = "#ffff23"      # Yellow for K/D/A stats
