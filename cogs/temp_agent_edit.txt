# Simple note: Add this to admin.py if you want manual agent correction

# @app_commands.command(name="edit-agent", description="[ADMIN] Correct a player's agent in their last match")
# @app_commands.describe(
#     player="The player to edit",
#     agent="The correct agent name"
# )
# async def edit_agent(self, interaction: discord.Interaction, player: discord.Member, agent: str):
#     """Manually correct agent detection for a player's most recent match"""
#     if not self.is_admin(interaction):
#         await interaction.response.send_message("❌ Admin only!", ephemeral=True)
#         return
#     
#     # TODO: Implement agent correction in match history
#     await interaction.response.send_message(f"✅ Updated {player.mention}'s agent to {agent}", ephemeral=True)
